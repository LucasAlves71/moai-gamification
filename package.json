{
  "name": "moai-gamification",
  "version": "0.0.0",
  "scripts": {
    "start": "http-server ./src -p 8080 -c-1 -o",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "private": true,
  "dependencies": {
    "angular": "^1.8.3",
    "angular-route": "^1.8.3",
    "bootstrap": "^5.3.0",
    "http-server": "^14.1.1"
  }
}
